const buttons=document.querySelectorAll(".choice");buttons.forEach(b=>b.addEventListener("click",()=>{buttons.forEach(x=>x.classList.remove("active"));b.classList.add("active")}));document.getElementById("enquiryForm").addEventListener("submit",e=>{e.preventDefault();document.getElementById("formMessage").textContent="Thanks! Your enquiry has been recorded. We will connect with you shortly.";e.target.reset();});